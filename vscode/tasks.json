{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "TmpFile",
            "type": "shell",
            "command": "/bin/bash /home/jk/dotfiles/vscode/tmp_file.sh",
            "problemMatcher": [],
            "presentation": {
                "echo": true,               // Display the task label when it runs
                "reveal": "never",          // Prevent the terminal from opening
                "focus": false              // Avoid focusing the terminal
            },
            "group": {
                "kind": "none"
            }
        }
    ]
}